<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/header') %> <!-- Ensure header includes any required metadata -->
    <link rel="stylesheet" href="/css/styles.css">
    <title>Guessing Game</title>
</head>
<body>
    <h1 class="heading">Card Guessing Game</h1>
    <div class="game-container">
        <div class="card-grid">
            <% for (let i = 0; i < 36; i++) { %>
                <div class="card" data-id="<%= i %>">
                    <div class="card-front">?</div>
                    <div class="card-back">Card <%= i %></div>
                </div>
            <% } %>
        </div>
        <button id="reset-btn">Reset Game</button>
        <div id="winning-message" style="display: none;">
            <p class="win">Congratulations! You've won!</p>
        </div>
    </div>
    <script src="/js/script.js" defer></script>
</body>
<footer class="footer">
    <%- include('../partials/footer') %>
</footer>
</html>
